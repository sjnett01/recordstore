<?php
require __DIR__.'/app-bootstrap.php';$error='';$sent=false;if($_SERVER['REQUEST_METHOD']==='POST'){require_csrf();$email=strtolower(trim((string)($_POST['email']??'')));if(filter_var($email,FILTER_VALIDATE_EMAIL)){request_email_otp($email,'password_reset');$_SESSION['password_reset_email']=$email;$sent=true;}}layout_header('Reset password');?><form class="auth panel" method="post"><span class="kicker">ACCOUNT SECURITY</span><h1>Reset password</h1><?php if($sent):?><p class="success">If that address is registered, a verification code has been sent.</p><label>Verification code<input name="code" inputmode="numeric" pattern="[0-9]{6}" maxlength="6" required></label><input type="hidden" name="csrf" value="<?=e(csrf_token())?>"><a class="button" href="<?=e(url('reset-password.php'))?>">Continue</a><?php else:?><p class="muted">Enter your account email to receive a verification code.</p><label>Email address<input type="email" name="email" autocomplete="email" required></label><input type="hidden" name="csrf" value="<?=e(csrf_token())?>"><button class="button">Send code</button><?php endif;?></form><?php layout_footer();
